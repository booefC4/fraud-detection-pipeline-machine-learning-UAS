# Python 3.11.9
# Install (CPU / GPU lama, compute capability <= sm_90):
#   pip install -r requirements.txt
#
# Install untuk GPU RTX 50-series / Blackwell (sm_120), termasuk RTX 5070 Ti:
#   JANGAN install torch dari baris di bawah ini. Install manual dulu:
#   pip uninstall -y torch torchvision torchaudio
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#   lalu install sisanya:
#   pip install numpy pandas scikit-learn lightgbm optuna mlflow matplotlib

numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1
lightgbm==4.3.0
optuna==3.6.1
mlflow==2.14.1
matplotlib==3.9.1
torch==2.3.1  # ABAIKAN baris ini kalau GPU kamu RTX 50-series (Blackwell) — lihat catatan di atas
